# 변경 내역

이 프로젝트의 사용자에게 영향을 주는 주요 변경 사항을 기록합니다.

## 0.4.3 - 2026-09-06

### 추가

- 플레이어 페이지에서 YouTube iframe으로 autoplay 권한을 명시적으로 위임합니다.
- 토큰이 포함된 로컬 플레이어 주소를 설정창에서 기본적으로 가리고 필요할 때만 표시할 수 있습니다.

### 변경

- 첫 곡은 음소거 자동재생으로 먼저 시작한 직후 설정 볼륨으로 복원해 클릭 요구 가능성을 낮췄습니다.
- 이전에 자동재생 플래그 없이 열린 브라우저 프로세스와 분리되도록 전용 프로필 경로를 갱신했습니다.
- 설정창을 상태, 재생 제어, 대기열, 자동 선곡, 이력, 로컬 연결 카드로 재구성했습니다.
- README를 사용자용 설치·사용 안내로 정리하고 변경 기록을 이 파일로 분리했습니다.

## 0.4.2 - 2026-09-06

### 추가

- RisuAI의 `@version` 및 `@update-url` 기반 플러그인 자동 업데이트를 활성화했습니다.
- 항상 최신 코드를 제공하는 `risu-plugin/contextual-youtube-bgm.plugin.js` 배포 파일을 추가했습니다.
- BAT 실행 시 helper token이 포함된 로컬 설치본을 `risu-plugin/local/`에 자동 생성하도록 했습니다.

### 변경

- 공개 저장소의 플러그인 원본과 고정 배포 파일은 항상 토큰 없는 상태로 유지합니다.
- 최초 로컬 설치에서 받은 helper token을 RisuAI 기기 로컬 저장소에 보존하여 원격 업데이트 후에도 연결을 이어받도록 했습니다.
- 플러그인, 도우미 및 플레이어 표기를 0.4.2로 통일했습니다.

### 보안

- 토큰 포함 로컬 플러그인 디렉터리를 Git 추적에서 제외했습니다.
- YouTube API 키, helper token, 재생 이력이 저장되는 로컬 파일은 배포 대상에서 제외됩니다.

## 0.4.1 - 2026-09-06

첫 공개 릴리스입니다.

### 주요 기능

- RisuAI API v3 플러그인과 로컬 YouTube 플레이어 연동
- 최근 대화 맥락을 사용하는 `otherAx` 보조 모델 선곡
- 곡·영상·아티스트 반복 방지 및 재생 이력 관리
- 8분 초과 영상과 Shorts 제외
- 개인 사용자의 가사·클린 음원 영상 우선 검색
- 공식·Topic·VEVO·라이브 영상 후순위 처리
- 재생 오류 및 시작 타임아웃 발생 시 다음 후보 자동 시도
- 원하는 곡 수동 대기열
- 두 플레이어를 이용한 자동 크로스페이드
- 드래그 위치가 저장되는 Now Playing 플로팅
- 전용 자동재생 브라우저와 플레이어 크기 설정
- 보조 모델 응답의 생각 블록·부가 문장 내 유효 JSON 복구

## 이전 개발 버전 호환성 기록

### 0.3.2

- 실제 재생 진행 증거가 없는 이전 이력 항목을 시작 시 폐기하는 마이그레이션을 추가했습니다.
- 재생되지 않은 예시 곡이나 실패 영상이 반복 차단 목록에 남는 문제를 방지했습니다.

### 0.1.1

- RisuAI `nativeFetch` 호출에 HTTP 메서드를 명시해 `Invalid body type` 오류를 수정했습니다.
